name: showcasing_flutter

description: Showcasing Flutter awesomeness. Purely for teaching & learning purpose. Flutter/Dart Tips & Tricks.
publish_to: "none"

version: 1.5.0+21

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^0.1.3
  geolocator: ^6.1.13
  geocoding: ^1.0.5
  provider: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  generate: true
  uses-material-design: true
